# Frontend

The frontend now hosts the first `ShopSim` sandbox storefront in React. It is intentionally lean, but already serves as the controlled UI surface for:

- recommendation explanations
- search and catalog browsing
- benchmark task display
- later approval checkpoints
- later execution traces and metrics panels
